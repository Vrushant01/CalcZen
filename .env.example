# Production API (Render) — used by Vite build and Vercel
VITE_API_URL=https://calczen.onrender.com

# Public site URL — used when generating sitemap.xml at build time
SITE_URL=https://www.calczen.in
